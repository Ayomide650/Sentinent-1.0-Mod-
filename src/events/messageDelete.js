```javascript
module.exports = {
  name: 'messageDelete',
  async execute(message) {
    // Log deleted message content, author, channel
    // Send to logging channel
    // ...existing code...
  }
};
```